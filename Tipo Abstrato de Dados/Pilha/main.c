#include <stdio.h>
#include "pilha.h"

int main() {
	
	Pilha a;
	a = Inicializar();
	int opcao = 0;
	while(opcao!=4)  {
		if(opcao == 1) {
			int elemento;
			printf("Qual elementa deseja inserir?\n>");
			scanf("%d",&elemento);
			
		}	
	}
	
}
